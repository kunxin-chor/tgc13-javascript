function multi(n1, n2) {
    return n1 * n2;
}

function distance(speed, time) {
    return time * speed; // assumption: if speed is km/h, then time is hours
}

function calculatePerimeter(length, width) {
    return length * width;
}